"form": {
    "enabled": true,
    "version": "2.1.0",
    "settings": {
        "timeout_ms": 300,
        "max_retries": 5,
        "log_level": "INFO"
    },
    "features": [
        "logging",
        "webhooks"
    ]
}

"migration": {
    "enabled": true,
    "version": "2.1.0",
    "settings": {
        "timeout_ms": 300,
        "max_retries": 2,
        "log_level": "WARNING"
    },
    "features": [
        "caching",
        "realtime"
    ]
}

"metrics": {
    "enabled": true,
    "version": "2.1.0",
    "settings": {
        "timeout_ms": 300,
        "max_retries": 5,
        "log_level": "INFO"
    },
    "features": [
        "logging",
        "export"
    ]
}

"middleware": {
    "enabled": true,
    "version": "2.1.0",
    "settings": {
        "timeout_ms": 15,
        "max_retries": 5,
        "log_level": "DEBUG"
    },
    "features": [
        "caching",
        "export"
    ]
}

"worker": {
    "enabled": true,
    "version": "2.1.0",
    "settings": {
        "timeout_ms": 30,
        "max_retries": 2,
        "log_level": "DEBUG"
    },
    "features": [
        "metrics",
        "webhooks"
    ]
}